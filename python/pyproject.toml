[project]
name = "render-cv"
version = "0.1.0"
description = "Minimal uv project for running RenderCV."
requires-python = ">=3.12"
dependencies = [
    "rendercv[full]>=2.8",
]

[tool.uv]
package = false
