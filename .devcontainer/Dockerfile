# Keep this patch version pinned: the floating `:debian` tag's 2.1.15 image broke
# Codex/bubblewrap `/proc` setup. See ../docs/codex-sandbox.md before upgrading.
FROM mcr.microsoft.com/devcontainers/base:2.1.11-trixie
